#[allow(dead_code)] // Will be used by the MCP server module.
mod analyzer;
mod errors;
#[allow(dead_code)] // Will be used by the MCP server module.
mod tidal;
#[allow(dead_code)] // Will be used by the MCP server module.
mod resources;
#[allow(dead_code)] // Will be used by the MCP server module.
mod tools;

fn main() {
    println!("mcp-server-tidal-cycles");
}
